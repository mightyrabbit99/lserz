# lserz

A single header C library that can be used to write serializer and deserializer efficiently by abstraction

### Installation

Because lserz is a single-header library, all you need to do is copy the `lserz.h` file into your project, and
include it at the top of your source file in which you would like to use it.
